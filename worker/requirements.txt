torch>=2.0.0
ultralytics>=8.3.0
# ultralytics 8.4+ 在 set_classes() 时硬要求 import clip（OpenAI CLIP 实现）
# 显式锁定 git 仓库依赖，避免运行时 auto-install 撞 Windows 权限
clip @ git+https://github.com/ultralytics/CLIP.git
transformers>=4.40.0
huggingface_hub>=0.20.0
albumentations>=1.4.0
opencv-python>=4.8.0
psutil>=5.9.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0

# =============================================================================
# Eagle/LocateAnything 可选依赖 (用于升级打标引擎)
# =============================================================================
# 安装方式: pip install -r requirements-eagle.txt
# 或: pip install -e ../Eagle-main/Embodied -e ../Eagle-main/Eagle2_5

# LocateAnything 依赖 (>= 6GB GPU)
# transformers==4.57.1
# tokenizers==0.22.0
# sentencepiece>=0.2.0
# accelerate>=1.5.2
# peft>=0.12.0
# timm>=1.0.11
# einops
# decord
# opencv-python-headless

# Eagle2.5 VQA 依赖 (>= 16GB GPU)
# transformers==4.51.0
# tokenizers==0.21.1
# peft>=0.12.0
# timm==1.0.11
# deepspeed>=0.15.0
# liger_kernel>=0.3.0
# torchcodec
# swanlab

# 可选：FlashAttention (加速推理)
# flash-attn --no-build-isolation

